var miso = require('./miso.js'),
	fs = require('fs');

fs.readFile('test.mi', 'utf8', function (err, data) {
	if (err) return;
	console.log(data);
	var ast = miso.parse(data.substring(0, data.length - 1));

	//console.log(ast[0].execute());
});

